version: '3'
services:
  uptest:
    image: image: h3rmt/uptest
    ports:
      - 8086:80
    volumes:
      - ./logs:/logs
      - ./responses:/responses
    environment:
      - URLS=www.google.com=google,www.yahoo.com=yahoo
